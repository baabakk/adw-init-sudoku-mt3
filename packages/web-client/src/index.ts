// web-client — Web Client
// Scope: Single-page browser application that renders an interactive 9x9 Sudoku board, accepts player input, validates moves client-side, fetches puzzles from the Puzzle Service, and posts completed-game results to the Scores Service.
// Owns: packages/web/**
// This team builds its slice here each phase.
export {};
